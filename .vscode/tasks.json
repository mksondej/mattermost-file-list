{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make server",
            "type": "shell",
            "command": "make server",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "make deploy",
            "type": "shell",
            "command": "make deploy",
            "group": "build"
        }
    ]
}
